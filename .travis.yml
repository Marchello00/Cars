language: cpp

os:
  - linux
  - osx

compiler:
  - gcc
  - clang

matrix:
  exclude:
    - os: osx
      compiler: gcc

script:
  - mkdir build
  - cd build
  - cmake ..
  - cmake --build

notifications:
  webhooks:
    - https://fathomless-fjord-24024.herokuapp.com/notify?secret=SuperEncryptedSecret
  email:
    - nagov-mark@mail.ru